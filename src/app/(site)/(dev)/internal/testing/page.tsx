import { InternalPage, InternalPageHeader } from "../_components/InternalPage";

export default function TestingPage() {
	return (
		<InternalPage>
			<InternalPageHeader
				title="Testing"
				description="Create a temporary child route for focused instance work, then remove it with the accepted change."
			/>
		</InternalPage>
	);
}

interface:
  display_name: "Averlo · Systemize Composition"
  short_description: "Promote reviewed composition roles with confidence"
  default_prompt: "Use $averlo:systemize-composition to analyze this accepted composition and migrate only confidence-supported roles while explaining relevant visual effects across consumers."
policy:
  allow_implicit_invocation: false

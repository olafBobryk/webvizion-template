interface:
  display_name: "Averlo · Compose"
  short_description: "Orchestrate exact static and optional motion delivery"
  default_prompt: "Use $averlo:compose to realize, integrate, and converge this source-backed surface, adding motion only when requested."
policy:
  allow_implicit_invocation: true

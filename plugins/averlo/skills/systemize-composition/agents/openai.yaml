interface:
  display_name: "Averlo · Systemize Composition"
  short_description: "Replace inherited owners from accepted compositions"
  default_prompt: "Use $averlo:systemize-composition to exhaustively replace inherited template recipes with accepted composition roles through their central instance owners. Preserve stable semantic APIs, not inherited pixels: correct unchanged template call sites should inherit central recipe replacements without caller migration, and every inherited public identifier must be remapped before a genuinely additional value is introduced. Do not let historical agent audits, task memory, preservation locks, or parallel variants shelter foreign template visuals. A repeated primary-action compound must consume the replaced Button primary role rather than add a compound-named primitive variant. Treat established Text, Logo, Button, repeated compound, Section-axis, media, route, and link ownership as deterministic automatic work rather than confidence recommendations; account for every required owner domain, protect evidenced conflicting prior product systemization, attempt every executable automatic row, keep blocked required rows incomplete, and stop with all changes uncommitted at the human-review checkpoint."
policy:
  allow_implicit_invocation: false

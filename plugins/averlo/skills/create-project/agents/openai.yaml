interface:
  display_name: "Averlo · Create Project"
  short_description: "Initialize a verified local Averlo project"
  default_prompt: "Use $averlo:create-project to create and verify a clean local Averlo project from the published initializer."

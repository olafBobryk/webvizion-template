interface:
  display_name: "Averlo · Systemize Composition"
  short_description: "Promote reviewed composition roles with confidence"
  default_prompt: "Use $averlo:systemize-composition to exhaustively replace inherited template recipes with accepted composition roles through their central instance owners, including every text role and exact identity asset, while protecting prior product systemization, separately accounting for every discovered candidate across every required owner domain, attempting every executable high-confidence row, and reporting any blocked high row or failed verifier as incomplete at the human-review checkpoint."
policy:
  allow_implicit_invocation: false
